<div class="container">
  <div class="d-flex justify-content-between my-3">
    <h1>Friends</h1>
    <div class="card-link"><%= link_to "Add New Friend +", new_friend_path, {class:' btn btn-outline-primary'} %></div>
  </div>
  <div class="row mb-3 g-3 row-cols-1 row-cols-xl-4 row-cols-lg-3 row-cols-sm-2">
    <%= render partial: 'friend', collection: @friends, as: :friend %>
  </div>
</div>

<%= render 'pagination', pagination: @pagination %>
